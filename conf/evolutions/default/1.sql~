# --- First database schema

# --- !Ups

use `bardemo`;


# -- DROP TABLE if exists bar;

CREATE TABLE `bardemo`.`bar` IF NOT EXISTS(
#  ---  `id`    INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
   `id`   INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
   `name` VARCHAR(128) NOT NULL);

# --- !Downs


